#!/bin/sh
set -e

case "${1:-}" in
    remove|purge)
        if command -v systemctl >/dev/null 2>&1; then
            systemctl --global disable gnomeai-whatsapp.service >/dev/null 2>&1 || true
        fi
        ;;
esac

exit 0
